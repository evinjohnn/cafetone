pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // CRITICAL FIX: Removed all custom/JitPack repositories.
        // The official Shizuku artifacts are on Maven Central.
    }
}

rootProject.name = "CaféTone"
include ':app'